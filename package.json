{
  "name": "rift",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A 2D platformer where you switch between two versions of the same level (PAST <-> FUTURE).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
